meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "name": "user",
    "password": "password"
  }
}

script:post-response {
  bru.setEnvVar("token",res.getBody().token)
}
