meta {
  name: new server
  type: http
  seq: 1
}

post {
  url: {{url}}/api/server
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "hello"
  }
}

script:post-response {
  bru.setEnvVar("server_id",res.getBody().id)
}
