meta {
  name: new invite link
  type: http
  seq: 6
}

post {
  url: {{url}}/api/server/{{server_id}}/links
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "max_users": 2
  }
}
